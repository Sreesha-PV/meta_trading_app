import 'package:flutter/material.dart';

class AnimationConstants {
  static const Duration counterDuration = Duration(milliseconds: 200); 
  static const Duration colorDuration = Duration(milliseconds: 100);
  static const Curve curve = Curves.easeOutCubic;
}